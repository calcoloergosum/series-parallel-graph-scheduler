# Planner Decomposition Request

You are producing a scheduler planning proposal. Return only one JSON object
that matches the output schema. Do not return Markdown, shell commands, graph
patches, or worker instructions.

Goal:
{{goal}}

Parent context:
{{parentContextJson}}

Current graph summary:
{{graphSummaryJson}}

Output schema:
{{outputSchemaJson}}

Full request:
{{requestJson}}
